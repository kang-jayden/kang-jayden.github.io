---
layout: post
title: Geographic Information Systems
is_series: true
series_title: "Skills"
---

## GIS Applications That I Use

### QGIS

This is my main platform. It is the most flexible, beginner-friendly, and hassle-free platform that I have tried. It integrates functions from SAGA and GRASS, which means you could even use plugins designed for those platforms. 

### Google Earth Engine

After learning the basics of Google Earth Engine, 

<!-- Series links -->
{% if page.is_series == true %}
<h3 class="text-success p-3 pb-0">{{ page.series_title | upcase }} Series</h3>
{% assign posts = site.posts | where: "is_series", true | where: "series_title", page.series_title | sort: 'date' %}
 
{% for post in posts %}
        {% if post.title == page.title %}
 <p class="nav-link bullet-pointer mb-0">{{ post.title }}</p>
        {% else %}
 <a class="nav-link bullet-hash" href="{{ post.url }}">{{ post.title }}</a>
        {% endif %}
{% endfor %}

{% endif %}